import React from 'react';
import './Loading.css'

const Loading = () => {
    return (
        <div id="loadingScreen">
            <div className="loader"/>
        </div>
    );
};

export default Loading;